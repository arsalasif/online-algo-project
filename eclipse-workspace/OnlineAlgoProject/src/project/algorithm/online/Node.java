package project.algorithm.online;

// A class for node for trees
public class Node {
    Integer key;            // key
    Node left, right;   // left and right subtrees

    public Node(int k) {
        key = k;
        left = null;
        right = null;
    }
    
}