package project.algorithm.online;

public class Node {
    Integer key;            // key
    Node left, right;   // left and right subtrees

    public Node(int k) {
        key = k;
        left = null;
        right = null;
    }
    
}